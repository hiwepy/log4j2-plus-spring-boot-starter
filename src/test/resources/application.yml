logging:
  log4j:
    jdbc:
      column-configs:
      - clob: false
        column: LOG_ID
        pattern: "%X{key}"
      - column: xxx
        pattern: "%d{yyyy-MM-dd HH:mm:ss.SSS}"
        